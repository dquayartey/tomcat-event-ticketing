AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Tomcat Events Limited - Event Registration API

Parameters:
  Environment:
    Type: String
    Default: dev
    AllowedValues:
      - dev
      - staging
      - prod
    Description: Deployment environment (dev, staging, prod)

Globals:
  Function:
    Runtime: python3.12
    Handler: app.lambda_handler
    Timeout: 10
    MemorySize: 128
    Environment:
      Variables:
        TABLE_NAME: !Ref EventTicketingTable
        ENVIRONMENT: !Ref Environment

Resources:
  EventTicketingApi:
    Type: AWS::Serverless::Api
    Properties:
      StageName: Prod
      Cors:
        AllowMethods: "'GET,POST,DELETE,OPTIONS'"
        AllowHeaders: "'Content-Type,Authorization,X-Amz-Date,X-Api-Key,X-Amz-Security-Token'"
        AllowOrigin: "'*'"

  EventTicketingTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Sub "tomcat-${Environment}-events"
      AttributeDefinitions:
        - AttributeName: PK
          AttributeType: S
        - AttributeName: SK
          AttributeType: S
        - AttributeName: GSI1PK
          AttributeType: S
        - AttributeName: GSI1SK
          AttributeType: S
      KeySchema:
        - AttributeName: PK
          KeyType: HASH
        - AttributeName: SK
          KeyType: RANGE
      GlobalSecondaryIndexes:
        - IndexName: EmailIndex
          KeySchema:
            - AttributeName: GSI1PK
              KeyType: HASH
            - AttributeName: GSI1SK
              KeyType: RANGE
          Projection:
            ProjectionType: ALL
      BillingMode: PAY_PER_REQUEST

  RegisterFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../src/handlers/register/
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref EventTicketingTable
      Events:
        RegisterApi:
          Type: Api
          Properties:
            RestApiId: !Ref EventTicketingApi
            Path: /register
            Method: POST

  ListEventsFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../src/handlers/list_events/
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref EventTicketingTable
      Events:
        ListEventsApi:
          Type: Api
          Properties:
            RestApiId: !Ref EventTicketingApi
            Path: /events
            Method: GET

  GetRegistrationsFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../src/handlers/get_registrations/
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref EventTicketingTable
      Events:
        GetRegistrationsApi:
          Type: Api
          Properties:
            RestApiId: !Ref EventTicketingApi
            Path: /registrations/{email}
            Method: GET

  CancelRegistrationFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../src/handlers/cancel_registration/
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref EventTicketingTable
      Events:
        CancelRegistrationApi:
          Type: Api
          Properties:
            RestApiId: !Ref EventTicketingApi
            Path: /registration/{id}
            Method: DELETE

Outputs:
  ApiUrl:
    Description: API Gateway endpoint URL
    Value: !Sub "https://${EventTicketingApi}.execute-api.${AWS::Region}.amazonaws.com/Prod"
  TableName:
    Description: DynamoDB Table Name
    Value: !Ref EventTicketingTable